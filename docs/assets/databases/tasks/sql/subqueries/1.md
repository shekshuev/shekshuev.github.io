### Вариант 1

**1. Укажите корректный порядок логического выполнения предложений (клауз) в SQL-запросе сервером базы данных:**

а) `SELECT` -> `FROM` -> `WHERE` -> `GROUP BY` -> `HAVING` -> `ORDER BY`

б) `FROM` -> `WHERE` -> `GROUP BY` -> `HAVING` -> `SELECT` -> `ORDER BY`

в) `FROM` -> `GROUP BY` -> `WHERE` -> `HAVING` -> `SELECT` -> `ORDER BY`

г) `SELECT` -> `WHERE` -> `GROUP BY` -> `HAVING` -> `ORDER BY` -> `FROM`

**2. Какое из утверждений относительно работы агрегатных функций является верным?**

а) Функция `COUNT(*)` игнорирует строки, содержащие значение `NULL` во всех столбцах.

б) Функция `SUM()` применима к любым типам данных, включая строковые.

в) Функция `COUNT(column_name)` не учитывает строки, в которых значение `column_name` равно `NULL`.

г) Агрегатные функции невозможно использовать без явного указания предложения `GROUP BY`.

**3. Какой тип соединения необходимо использовать для возврата всех записей из левой таблицы и только совпадающих записей из правой таблицы (при отсутствии совпадений в правой таблице возвращаются значения `NULL`)?**

а) `INNER JOIN`

б) `FULL OUTER JOIN`

в) `CROSS JOIN`

г) `LEFT JOIN`

**4. Анализ результата выполнения запроса**

Дана таблица `monthly_sales` (Результаты продаж отделов за месяц):

| id  | manager_name | department | revenue | transactions_count |
| --- | ------------ | ---------- | ------- | ------------------ |
| 1   | Иванов А.    | Alpha      | 120.50  | 5                  |
| 2   | Смирнов В.   | Beta       | 80.00   | 2                  |
| 3   | Кузнецов И.  | Alpha      | 210.00  | 12                 |
| 4   | Попов С.     | Gamma      | 50.00   | 1                  |
| 5   | Васильев Д.  | Beta       | 150.00  | 8                  |
| 6   | Петров Н.    | Alpha      | 90.00   | 4                  |
| 7   | Соколов М.   | Gamma      | 300.00  | 20                 |
| 8   | Михайлов Т.  | Beta       | 110.00  | 6                  |
| 9   | Федоров П.   | Alpha      | 450.00  | 25                 |
| 10  | Морозов К.   | Gamma      | 95.00   | 3                  |
| 11  | Волков Е.    | Beta       | 200.00  | 10                 |
| 12  | Алексеев Р.  | Alpha      | 60.00   | 2                  |
| 13  | Лебедев В.   | Gamma      | 180.00  | 9                  |
| 14  | Семенов Д.   | Beta       | 40.00   | 1                  |
| 15  | Егоров А.    | Alpha      | 130.00  | 7                  |

_Текст запроса:_

```sql
SELECT department, COUNT(manager_name) AS active_managers, SUM(revenue) AS total_revenue
FROM monthly_sales
WHERE revenue > 100
GROUP BY department
HAVING SUM(transactions_count) >= 15
ORDER BY total_revenue DESC;
```

**Задание:** Опишите итоговый результат выполнения данного запроса (какие строки и значения в столбцах будут выведены на экран). Обоснуйте свой ответ, пошагово исключая строки.

**5. Поиск синтаксической ошибки**

Дан SQL-запрос, предназначенный для вывода названия отдела, должности сотрудника и средней зарплаты по этой должности внутри отдела. При выполнении СУБД возвращает синтаксическую ошибку.

```sql
SELECT department_id, job_title, AVG(salary)
FROM employees
GROUP BY department_id
ORDER BY AVG(salary) DESC;
```

**Задание:** Укажите причину возникновения ошибки и напишите исправленный вариант запроса.

**6. Дополнение запроса**

Требуется вывести список наименований всех контрагентов (таблица `contractors`), с которыми за текущий год не было заключено ни одного договора (таблица `contracts`).
**Задание:** Заполните пропуски в предоставленном SQL-коде:

```sql
SELECT c.contractor_name
FROM contractors c
<       > JOIN contracts con ON c.id = con.contractor_id
WHERE con.id <                          >;
```

**7. Поиск логической ошибки**

Аналитику была поставлена задача: вывести наименования товаров из категорий «Электроника» и «Бытовая техника», цена которых строго превышает 15000 условных единиц. Написанный им запрос отрабатывает без синтаксических ошибок, но возвращает некорректный набор данных (в том числе дешевую электронику).

```sql
SELECT product_name, category, price
FROM products
WHERE category = 'Электроника' OR category = 'Бытовая техника' AND price > 15000;
```

**Задание:** Объясните причину логической ошибки, опираясь на приоритет операторов SQL, и приведите исправленный вариант кода.

**8. Анализ результата выполнения запроса**

Дана таблица `athlete_scores` (Результаты соревнований):

| id  | athlete_name | score |
| --- | ------------ | ----- |
| 1   | Сидоров      | 100   |
| 2   | Петров       | 100   |
| 3   | Иванов       | 95    |
| 4   | Смирнов      | 90    |
| 5   | Кузнецов     | 90    |
| 6   | Попов        | 90    |
| 7   | Васильев     | 85    |
| 8   | Соколов      | 80    |
| 9   | Михайлов     | 80    |
| 10  | Федоров      | 75    |
| 11  | Морозов      | 70    |
| 12  | Волков       | 60    |

_Текст запроса:_

```sql
SELECT athlete_name, score,
DENSE_RANK() OVER (ORDER BY score DESC) as rank_position
FROM athlete_scores;
```

**Задание:** Укажите, какое значение будет выведено в столбце `rank_position` для атлета «Васильев» (score 85) и для атлета «Соколов» (score 80). Чем поведение `DENSE_RANK()` в данном случае будет отличаться от функции `RANK()`?

**9. Дополнение запроса**

Необходимо рассчитать нарастающий итог (running total) ежедневной выручки в рамках каждого отдельного филиала.
**Задание:** Заполните пропуски в предоставленном SQL-коде для корректной работы оконной функции:

```sql
SELECT branch_id, operation_date, daily_revenue,
SUM(daily_revenue) OVER (<              > branch_id <              > operation_date) AS running_total
FROM daily_sales;
```

**10. Проектирование схемы базы данных «Медицинская клиника»**

Спроектируйте структуру базы данных (ERD-схему), состоящую минимум из 4-5 таблиц.

**Требования предметной области:**

- В клинике работают Врачи (у врача есть специализация, ФИО, кабинет).
- В клинику обращаются Пациенты (ФИО, дата рождения, полис ОМС).
- Врачи осуществляют Приемы пациентов (каждый прием имеет дату, время, диагноз).
- В рамках одного приема пациенту может быть назначено несколько Медикаментов (рецепт). Один и тот же медикамент может назначаться на разных приемах разным пациентам.

**Задание:**

1. Перечислите названия создаваемых сущностей (таблиц).
2. Для каждой таблицы укажите атрибуты (поля), выделив первичные ключи (PK) и внешние ключи (FK).
3. Опишите тип связи между сущностями (1:M, M:N). Обратите особое внимание на связь между «Приемами» и «Медикаментами».
